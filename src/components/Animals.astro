---
import { getAnimals } from '../services/caas-scrapping';
import CardAnimal from './CardAnimal.astro';

const animals = await getAnimals()
---
<div class="grid gap-8 sm:grid-cols-2 md:grid-cols-3">
  {animals.map(animal => 
    <CardAnimal animal={animal} />
  )}
</div>